'use strict';

var app = require('..');
import request from 'supertest';
import co from 'co';

describe('Fixture API:', function() {
  describe('GET /error', function() {
    /*
    it('should return a 500', function(done) {
      request(app)
        .get('/error')
        .expect(500)
        .end(done);
    });
    */
  });
});
